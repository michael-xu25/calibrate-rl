# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Model checkpoints (too large for git)
checkpoints/

# Large intermediate eval files — not needed for training, rebuild with run_evaluation.py
data/evaluation_results*.json
data/preeval_*.json
data/probe_*.json
logs/

# Training inputs (goldilocks inventories, pool, heldout) are tracked —
# they're needed on Lightning AI and are small enough for git (< 500KB each)

# Environment
.env

# IDE
.cursor/
.vscode/

# OS
.DS_Store
